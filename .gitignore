node_modules
dist
dist-ssr
*.local
CLAUDE.local.md
# Lokalne notatki robocze (np. docs/SCRUB-LOG.local.md) — nigdy do repo.
*.local.md
.DS_Store
.vite

# Agent worktrees live inside the repo (.claude/worktrees/<name>). They were
# only hidden by .git/info/exclude, which is machine-local and never shared.
.claude/worktrees/

# Local state from `wrangler dev` (the .claude/launch.json "sandstr-workers"
# config) — caches and a simulated asset store, none of it reproducible input.
.wrangler

# Reference recordings/screenshots of the real clients (docs/FIDELITY.md).
# Local-only by owner decision (2026-07-28): they are working reference
# material, not something to publish. Purged from git history the same day —
# do not re-add them. The screen-map.md specs STAY tracked.
docs/refs/*/shots/

# tsc incremental state (a `composite`/`--build` run drops these next to the config)
*.tsbuildinfo

# Working notes that are not part of the published project. Local only.
docs/grants/
docs/SHIP-AND-GRANT.md
docs/GRANT-WOW.md
docs/AUDIT.md
