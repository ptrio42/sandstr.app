# ffmpeg intermediates — every segment is regenerated by build-teaser.sh
.work/

# FAQ cut outputs stay out of the repo: they are rebuilt from the captured loops
# by build-teaser-faq.sh in about a minute, and the cut is still being reworked.
# (The first teaser's two files predate this and stay tracked.)
out/sandstr-faq-*.mp4
out/sandstr-tour-*.mp4
