# Nothing binary in here is tracked (2026-08-19). The recordings are the owner's
# own screen captures, the cuts and stills are build output, and together they
# were half the weight of the repository's history. All of it stays local; the
# scripts next to this file rebuild the cuts from the recording in ~3 minutes.
*.mov
*.MOV
*.mp4
*.MP4
*.webm
*.png
*.jpg
*.jpeg

# ffmpeg intermediates — every segment is regenerated by build-teaser.sh
.work/
out/
shots/
